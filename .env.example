# Base URL of the REST backend (e.g. Quarkus service).
# All requests issued by src/services/HttpApiClient.ts are prefixed with this.
# Leave empty to force seed-data mode (useful for offline UI work).
VITE_API_URL=http://localhost:8080/api
